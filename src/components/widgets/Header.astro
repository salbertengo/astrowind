---


import Image from '~/components/common/Image.astro';

import { getHomePermalink } from '~/utils/permalinks';
import { trimSlash, getAsset } from '~/utils/permalinks';
import type { CallToAction } from '~/types';
import { headerData } from '~/navigation';

interface Link {
  text?: string;
  href?: string;
  ariaLabel?: string;
  icon?: string;
}

interface MenuLink extends Link {
  links?: Array<MenuLink>;
}

export interface Props {
  id?: string;
  links?: Array<MenuLink>;
  actions?: Array<CallToAction>;
  isSticky?: boolean;
  isDark?: boolean;
  isFullWidth?: boolean;
  showToggleTheme?: boolean;
  showRssFeed?: boolean;
  position?: string;
}

const {
  id = 'header',
  isDark = false,

} = Astro.props;



<<<<<<< Updated upstream
<<<<<<< Updated upstream
const headerInferiorLinks = [
  { text: 'Uses', href: '/' },
  { text: 'Industries', href: '/' },
  { text: 'Size', href: '/' },
  { text: 'Contact', href: '/' },
];
=======

>>>>>>> Stashed changes
=======

>>>>>>> Stashed changes
---

<!-- Header Superior -->
<header
  class:list={[
    { dark: isDark },
    'relative z-40 flex-none mx-auto w-full border-b border-gray-50/0',
  ]}
  {...id ? { id } : {}}
>
  <div class="absolute inset-0 bg-[#030620]"></div>
  
  <div class="relative text-white w-[1440px] h-[132px] mx-auto py-[32px] px-[80px] flex items-center">
    <div class="flex justify-between items-center w-full">
      <a class="flex items-center" href={getHomePermalink()}>
        <Image 
          src="~/assets/images/logopampa.png" 
          alt="Pampa&Código"
          class="w-[170px] h-[68px] object-contain"
        />
      </a>
      
      <div class="flex items-center h-11">
        <nav
          class="items-center w-auto hidden md:flex"
          aria-label="Main navigation"
        >
          <ul class="flex items-center w-[398px] h-[44px] gap-[40px]">
            {
              [
                { text: 'Services', href: '/services' },
                { text: 'Blog', href: '/blog' },
                { text: 'Contact Us', href: '/contact' }
              ].map(({ text, href }, index) => (
                <li class="flex items-center">
                  <a
                    class="font-epilogue font-normal text-[20px] leading-[120%] text-white hover:text-white/80 transition-colors duration-200"
                    href={href}
                  >
                    {text}
                  </a>
                </li>
              ))
            }
          </ul>
        </nav>
      </div>
    </div>
  </div>
</header>

<!-- Mobile navigation - solo visible en pantallas pequeñas -->
<div class="md:hidden bg-[#030620] text-white py-4">
  <div class="container mx-auto px-4">
    <ul class="flex flex-col items-center gap-4">
      {
        [
          { text: 'Services', href: '/services' },
          { text: 'Blog', href: '/blog' },
          { text: 'Contact Us', href: '/contact' }
        ].map(({ text, href }) => (
          <li>
            <a 
              class="font-epilogue font-normal text-[18px] leading-[120%] text-white hover:text-white/80"
              href={href}
            >
              {text}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</div>
